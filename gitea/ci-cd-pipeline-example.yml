# Gitea CI/CD Example Pipeline
# This is a basic example of a Gitea Actions workflow file.
# Place this in your Gitea repository as .gitea/workflows/ci.yml

name: CI
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Run a one-line script
        run: echo "Hello from Gitea CI/CD!"
